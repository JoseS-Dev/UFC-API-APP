export interface UserData {
    name_user: string;
    username_user: string;
    email_user: string;
    password_user: string;
}

export interface LoginData {
    email_user: string;
    password_user: string;
}